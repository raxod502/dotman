targets:
  test: {}
  install:
    verifier: test
  configure:
    depends: [ install ]
  run:
    depends: [ configure ]

commands:
  install: {}
  uninstall:
    target: install
    mode: reverse
  upgrade:
    target: install
    mode: upgrade
  configure: {}
  unconfigure:
    target: configure
    mode: reverse
  run: {}
