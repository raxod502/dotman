# Statefile specification

The state file shall be saved in `$XDG_CONFIG_HOME/dotman/state.yml`.

## Contents

* For all declared targets, whether they are configured and/or
  installed, and with what options.
